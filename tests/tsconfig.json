{
    "extends"        : "../tsconfig.base.json",
    "compilerOptions": {
        "module" : "CommonJS",  /* node needs CommonJS */
        "outDir" : "../out",
        "baseUrl": ".",
        "paths"  : {
            "@/*"   : [ "../src/ts/*" ],
            "@svc/*": [ "../src/ts/services/*" ]
        }
    }
    /* dont put any reference to src here, as *.vue can't be built by typescript alone */
}
